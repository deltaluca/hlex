To compile, requires caxe preprocessor, and scx caxe library (another github repo)

them simply invoke the makefile, making sure that $SCX env-var is set up to point to scx root.

to execute hlex, you must have env-var $HLEX_ROOT set up to point to the installation directory of hlex containing the bin and scripts folders.
